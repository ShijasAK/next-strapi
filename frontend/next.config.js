/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["api.rel8hr.com",'rel8hrdev.e8demo.com','localhost'],
  },
  poweredByHeader: false,
};

module.exports = nextConfig;
